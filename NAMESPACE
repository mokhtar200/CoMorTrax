# Generated by roxygen2: do not edit by hand
# (Manually curated for initial Bioconductor submission)

## ── Imports ──────────────────────────────────────────────────────────────────

import(methods)
import(SingleCellExperiment)
import(SummarizedExperiment)

importFrom(BiocGenerics, counts, colnames, rownames)
importFrom(BiocParallel, bplapply, MulticoreParam, SerialParam)
importFrom(Matrix, Matrix, sparseMatrix, rowSums, colSums)
importFrom(S4Vectors, DataFrame, SimpleList, metadata, "metadata<-")
importFrom(SingleCellExperiment, logcounts, "logcounts<-", reducedDim, "reducedDim<-")
importFrom(SummarizedExperiment, colData, "colData<-", assay, rowData)
# clusterProfiler and org.Hs.eg.db are Suggests — accessed via .require_pkg()
importFrom(glmnet, cv.glmnet)
importFrom(igraph, graph_from_adjacency_matrix, V, E, degree, betweenness,
           communities, cluster_louvain, induced_subgraph, edge_attr,
           vertex_attr, as_adjacency_matrix)
importFrom(methods, new, validObject, is, slot, "slot<-", callNextMethod)
# (org.Hs.eg.db is Suggests — accessed via .require_pkg())
importFrom(pROC, roc, auc)
importFrom(randomForest, randomForest, importance)
importFrom(scater, runPCA, runUMAP, plotReducedDim)
importFrom(scran, computeSumFactors, modelGeneVar, getTopHVGs)
importFrom(scuttle, perCellQCMetrics, perFeatureQCMetrics)
importFrom(stats, wilcox.test, p.adjust, cor, cor.test, predict,
           prcomp, setNames, model.matrix, as.formula, median,
           quantile, sd, var, runif, rnorm, rbinom, rpois)
importFrom(utils, head, tail, write.csv, packageVersion, sessionInfo)
importFrom(viridis, scale_color_viridis, scale_fill_viridis, viridis_pal)
importFrom(ggplot2, ggplot, aes, aes_string, geom_point, geom_bar,
           geom_tile, geom_hline, geom_vline, geom_text, geom_col,
           geom_boxplot, geom_density, theme, theme_bw, theme_minimal,
           element_text, element_line, element_rect, element_blank,
           scale_color_manual, scale_fill_manual, scale_x_continuous,
           scale_y_continuous, scale_size_continuous, labs, ggtitle,
           xlab, ylab, facet_wrap, facet_grid, coord_flip, guides,
           guide_legend, guide_colorbar,
           scale_fill_viridis_c, scale_color_viridis_c,
           scale_fill_viridis_d, scale_color_viridis_d)

## ── S4 Class Exports ─────────────────────────────────────────────────────────

exportClasses(CoMorTraxObject)

## ── S4 Generic Exports ───────────────────────────────────────────────────────

# Accessor generics
exportMethods(
  diseaseMatrix,
  "diseaseMatrix<-",
  diseaseGroups,
  "diseaseGroups<-",
  deResults,
  "deResults<-",
  interactionScores,
  "interactionScores<-",
  pathwayResults,
  "pathwayResults<-",
  cellVulnerability,
  "cellVulnerability<-",
  networks,
  "networks<-",
  classifier,
  "classifier<-"
)

# Analysis generics
exportMethods(
  createCoMorTraxObject,
  preprocessCoMorTrax,
  encodeDiseaseLabels,
  runComorbidityDE,
  quantifyInteractionEffects,
  runComorbidityPathways,
  scoreCellVulnerability,
  buildComorbidityNetworks,
  trainComorbidityClassifier,
  mapSpatialComorbidity,
  plotCoMorTrax,
  summaryCoMorTrax,
  exportCoMorTrax,
  show
)

## ── Standard Function Exports ────────────────────────────────────────────────

export(
  # Accessor convenience functions
  getDeTable,
  getSynergisticGenes,
  getAntagonisticGenes,
  getAdditiveGenes,
  getNetworkHubs,
  listComorbidityComparisons,
  comparePathways,
  computeRewiring,
  plotSpatialComorbidity,
  validateCoMorTrax,
  # Utility functions
  simulateCoMorTrax
)
